{# Layout #}
{% extends "partials/layout.twig" %}

{# Content #}
{% block content %}
  <div class="vc-welcome">
    <div class="jumbotron jumbotron-fluid">
      <div class="container text-center">
        <h1 class="display-3">{{ config.client.name }}</h1>
        <p class="lead">Skeleton for {{ 'nodejs'|toUpper }}.</p>
      </div>
    </div>
  </div>
{% endblock %}
